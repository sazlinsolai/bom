<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>

<!--%= @pdf = Prawn::Document.new 
      @pdf.text "Hello Third"
      @pdf.font "Helvetica"
      @pdf.font.size = 13
      @pdf.render %-->


<%= 
          @pdf = Prawn::Document.new 
          @pdf.table [ ["#{@product.product_name}", "#{@product.product_no}"]] * 10,
          :position => :center,
          :headers => ['Product Name', 'Product No'],
          :widths => { 0 => 100, 1 => 100},
          :border_style => :grid,
          :border_color => "ff0000"
          @pdf.render
%>

 